[package]
name = "astral-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "astral"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
astral-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
